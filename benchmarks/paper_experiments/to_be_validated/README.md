# to_be_validated

Static catalog of all paper experiments, grouped by SSL technique (`tfc/`, `lfr/`, `tnc/`, `diet/`) plus `ablations/`; copy an experiment from here into `validation_run/` to run it.

# validated

Finished experiments promoted from `validation_run/`. The `saved_metrics_*.csv` files produced by `summarizer.py` are written to the `benchmarks/` directory; copy or move the relevant ones here alongside their experiment if you want to keep results and configs together.
